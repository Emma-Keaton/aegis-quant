# Aegis Quant — Frontend environment (Vercel)
# Copy to .env.local for local dev; set on Vercel project.

# Public API base URL (no trailing slash). Leave empty for same-origin (dev).
VITE_API_URL=http://localhost:8000

# WalletConnect Cloud project ID
VITE_WALLET_CONNECT_PROJECT_ID=
