# Core
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv

# Database
sqlalchemy
asyncpg
psycopg2-binary

# Redis
redis

# Exchanges
ccxt
solana
google-generativeai

# Scheduling
apscheduler

# Crypto
cryptography

# HTTP
httpx

# ML / data (Kronos runs on a separate service, so torch/transformers are NOT
# installed here — keep this service lean on memory).
pandas
numpy

# Optional: statistical forecasting fallback tier (ETS/Holt-Winters). Disabled by
# default to stay lean; the deterministic tier is used instead via a guarded import.
# statsmodels>=0.14.0

# Social scraping (Engine B)
feedparser
telethon
twikit

# Utilities
orjson
python-multipart>=0.0.7

# Solana
solana
solders
base58

# TON (autonomous server-side transfers via TON_MNEMONIC)
tonsdk

# Monitoring
prometheus-client

# AI Services
groq>=0.4.0
